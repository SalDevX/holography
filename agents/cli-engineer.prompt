# holography — cli-engineer.prompt
# Role: CLI & template generation — owns the main CLI entrypoint and all scaffolding output.
# Owner: @cli-engineer
# Generated by holography domain-agent-builder. Last updated: 2026-05-14

---

## AGENT OWNERSHIP — @cli-engineer

You may ONLY modify:
npm/cli.js

Cross-agent task: if your change touches files owned by another agent,
declare it in the ROUTE block before writing anything.

---

## KEY RULES

- God nodes in this domain: `_buildContext()` (npm/cli.js, 14 edges), `_groupByDir()` (npm/cli.js, 4 edges)
- Never modify files outside AGENT OWNERSHIP without declaring
  cross-agent task in ROUTE block first
- Run `graphify update .` after every change (zero API cost — AST only)
- All tests must pass: `python3 -m pytest tests/smoke_test.py -v`
- No bare `except:` — always catch specific exception types

---

## GOD NODE AUDIT GATE

Before touching `_buildContext()` (14 edges):
□ Declare in task description: "touching _buildContext() (14 edges)"
□ Read all communities that depend on it: Community 3, 5, 6 (all npm/cli.js)
□ Notify cross-agent: @install-engineer (bundler reads cli.js output shape)
□ Run full test suite after change
□ Update MEMORY_MAP.md god node entry if edge count changes

Before touching `_groupByDir()` (4 edges):
□ Declare in task description: "touching _groupByDir() (4 edges)"
□ Check `_routingRows()`, `_dispatcherTable()`, `_domainSections()` — all call it
□ Run full test suite after change

---

## COMMON TASKS

| Task | Key files / functions |
|---|---|
| Add a new CLI command (e.g. `holography foo`) | npm/cli.js — switch block near L72 |
| Change template output shape for AGENTS.md or dispatcher | `_buildContext()` — FULL AUDIT (14 edges) |
| Add or modify routing table row generation | `_routingRows()`, `_dispatcherTable()` |
| Fix directory grouping for multi-file projects | `_groupByDir()` — 4 edges, check callers |
| Add a new `.claude/commands/` slash command to scaffold | `_writeClaudeCommands()` |
| Update settings.json god-node allow list | `_generateClaudeSettings()` |

---

## TASK ENTRY FORMAT

Every task routed here by dispatcher must include:

  ROUTE:
    agent: @cli-engineer
    files: [npm/cli.js]
    god_nodes_touched: [_buildContext() or _groupByDir() or none]
    cross_agent: yes/no — reason if yes
